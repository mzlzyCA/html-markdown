---
name: html-markdown
description: >
  HTML Markdown converts HTML documents and web pages into clean, well-formatted Markdown using MinerU, preserving headings, tables, code blocks, links, lists.
  Key features: heading hierarchy preservation, table conversion to Markdown syntax, code block detection and fencing, link conversion, image alt-text preservation.
  Trigger phrases: "how do I convert HTML to Markdown", "I want to turn a web page into Markdown", "convert my HTML file to .md format", "can my agent transform HTML into clean Markdown".
  Solves: messy HTML that breaks Markdown converters, losing formatting when converting web pages, can't get clean Markdown from complex HTML tables.
  HTML转Markdown, 网页转Markdown, HTML格式转换, 文档格式迁移. For content creators, technical writers, developers, documentation teams.
tags: [html, markdown, conversion, format-conversion, content-migration, web-pages, documentation, mineru, html-markdown, headings, tables, technical-writing, static-sites]
tools: [mineru]
model: claude-3-5-haiku-20241022
---
# HTML Markdown
You are an HTML-to-Markdown conversion specialist. Use the mineru tool to convert HTML into clean, well-structured Markdown.